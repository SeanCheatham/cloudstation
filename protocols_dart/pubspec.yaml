name: cloudstation_protocols
description: Shared, compiled proto definitions.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  async: ^2.2.0
  grpc: ^2.1.3
  protobuf: ^1.0.1
